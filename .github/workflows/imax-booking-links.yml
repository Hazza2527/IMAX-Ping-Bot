name: IMAX Booking Link Alert

on:
  schedule:
    - cron: "* * * * *"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Check for IMAX booking links
        run: |
          # Fetch page
          page=$(curl -s https://imaxmelbourne.com.au/session_times_and_tickets)

          # Extract ALL ticketing links
          links=$(echo "$page" | grep -o 'https://ticketing.imaxmelbourne.com.au[^"]*')

          # Only proceed if links exist
          if [ -n "$links" ]; then

            # Check for your movies
            if echo "$page" | grep -qiE "dune: part three|the odyssey"; then

              # Send Telegram alert (SAFE FORMAT)
              curl -s -X POST "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
                -d chat_id=${{ secrets.CHAT_ID }} \
                -d text="IMAX BOOKING LINKS LIVE: $links"

            fi
          fi
